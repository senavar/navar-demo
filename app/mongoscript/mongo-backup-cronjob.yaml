---
apiVersion: batch/v1
kind: CronJob
metadata:
  name: mongo-backup
  namespace: birthdayapp
  labels:
    app: mongo-backup
spec:
  schedule: "0 2 * * *"  # daily at 02:00 UTC
  concurrencyPolicy: Forbid
  successfulJobsHistoryLimit: 3
  failedJobsHistoryLimit: 3
  jobTemplate:
    spec:
      backoffLimit: 1
      template:
        metadata:
          labels:
            app: mongo-backup
            azure.workload.identity/use: "true"  
        spec:
          serviceAccountName: birthdayapp-sa
          restartPolicy: Never
          securityContext:
            runAsNonRoot: true
            runAsUser: 10001
            runAsGroup: 10001
            fsGroup: 10001
            seccompProfile:
              type: RuntimeDefault
          containers:
            - name: backup
              image: acrdevnavarlabh7jl.azurecr.io/mongodb/backupscript:dev
              imagePullPolicy: IfNotPresent
              env:
                - name: MONGO_URI_FILE
                  value: /mnt/secrets-store/mongo-conn-string
                - name: MONGO_DB
                  value: birthdays_db
                - name: BACKUP_PREFIX
                  value: mongo-backup
                - name: RETENTION
                  value: "7"
                - name: AZURE_STORAGE_ACCOUNT_NAME
                  value: stdevnavarlabh7jl  
                - name: AZURE_STORAGE_CONTAINER
                  value: db-backups
              volumeMounts:
                - name: mongo-conn
                  mountPath: /mnt/secrets-store
                  readOnly: true
              resources:
                requests:
                  cpu: 50m
                  memory: 128Mi
                limits:
                  cpu: 200m
                  memory: 256Mi
              securityContext:
                allowPrivilegeEscalation: false
          volumes:
            - name: mongo-conn
              csi:
                driver: secrets-store.csi.k8s.io
                readOnly: true
                volumeAttributes:
                  secretProviderClass: app-secrets
