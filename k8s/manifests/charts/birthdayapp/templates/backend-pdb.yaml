apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: backend-pdb
  namespace: {{ include "birthdayapp.namespace" . }}
spec:
  minAvailable: 1
  selector:
    matchLabels:
      app: backend
